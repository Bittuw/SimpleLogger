#ifndef SIMPLE_FUNCTIONS
#define SIMPLE_FUNCTIONS

#include <string>

#include "Proxy/Proxy.hpp"

namespace simplefunctions {
    /*simpleproxy::Proxy log_impl(
        simplemessage::MessageType message_type, 
        std::string&& file,
        std::string&& func,
        const int line
        ) 
    {
        return {{message_type, file, func, line}};
    }*/
}

#endif